# Allowlist egress proxy.
# Default = DENY. Only the destinations needed for development are reachable.
# This is the single control that lets the agent build/install/commit while
# making exfiltration of sensitive data (e.g. from target/) impossible.

http_port 3128

# --- Allowlisted destinations ---------------------------------------------
# Java / Maven
acl allowed dstdomain repo.maven.apache.org repo1.maven.org
acl allowed dstdomain .maven.org .apache.org dlcdn.apache.org
# Source control
acl allowed dstdomain github.com .github.com codeload.github.com
acl allowed dstdomain .githubusercontent.com
# OS packages (Debian/Ubuntu base images)
acl allowed dstdomain deb.debian.org security.debian.org .debian.org
acl allowed dstdomain archive.ubuntu.com security.ubuntu.com ports.ubuntu.com
# Agent tooling (pip / npm)
acl allowed dstdomain pypi.org files.pythonhosted.org
acl allowed dstdomain registry.npmjs.org .npmjs.org
# Remote model endpoint(s) — add YOUR sovereign/EU inference host here:
# acl allowed dstdomain inference.your-eu-cloud.example

# --- Safety rails ----------------------------------------------------------
acl SSL_ports port 443
acl CONNECT method CONNECT
# Only allow HTTPS tunnels to allowlisted hosts.
http_access deny CONNECT !SSL_ports
http_access allow allowed
http_access deny all

# Don't leak proxy/host details downstream.
via off
forwarded_for delete
httpd_suppress_version_string on
