# Secrets — never committed
.env

# Host CA bundle copied at build time for this environment
corp-ca.crt

# Local model weights
.ollama/

# Build output — ephemeral, lives only inside the container volume
sample-app/target/
